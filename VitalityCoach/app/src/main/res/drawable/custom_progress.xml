<layer-list xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Define the background properties like color etc -->
    <item android:id="@android:id/background">
        <shape>
            <gradient
                android:startColor="#a39f9c"
                android:centerColor="#a39f9c"
                android:centerY="1.0"
                android:endColor="#a39f9c"
                android:angle="270"
                />
        </shape>
    </item>

    <!-- Define the progress properties like start color, end color etc -->
    <item android:id="@android:id/progress">
        <clip>
            <shape>
                <gradient
                    android:startColor="#ca5d0f"
                    android:centerColor="#ca5d0f"
                    android:centerY="1.0"
                    android:endColor="#a39f9c"
                    android:angle="270"
                    />
            </shape>
        </clip>
    </item>
</layer-list>